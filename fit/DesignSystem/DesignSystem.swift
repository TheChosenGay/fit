import SwiftUI

// MARK: - Design System Namespace

@available(iOS 15.0, *)
enum DesignSystem {
    typealias Spacing = DSSpacing
    typealias CornerRadius = DSCornerRadius
    typealias TextStyle = DSTextStyle
    typealias Shadow = DSShadow
}
